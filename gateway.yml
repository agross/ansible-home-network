- name: Configure host as a gateway
  hosts: gateway:&{{ site | default('home') }}
  become: true
  roles:
    - name: dotfiles
      tags: dotfiles
    - name: borgmatic
      tags: borgmatic
    - name: kernel-cmdline
      tags: kernel-cmdline
    - name: watchdog
      tags: watchdog
    - name: postfix
      tags: postfix
    - name: gateway
      tags: gateway
