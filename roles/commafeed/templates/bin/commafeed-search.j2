#!/usr/bin/env sh
# vim: set ft=jinja-shell ts=2 sw=2 et:

script="SELECT content
        FROM feedentrycontents
        WHERE content LIKE '%${1:?Need search string}%';"

docker compose \
       --project-directory {{ commafeed_root | ansible.builtin.quote }} \
       exec \
       db \
       psql --command "$script"
