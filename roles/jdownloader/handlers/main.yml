- name: Restart jdownloader service
  when: jdownloader_started is not changed
  ansible.builtin.systemd:
    name: jdownloader.service
    state: restarted

- name: Reload jdownloader service
  when: jdownloader_started is not changed
  ansible.builtin.systemd:
    name: jdownloader.service
    state: reloaded
