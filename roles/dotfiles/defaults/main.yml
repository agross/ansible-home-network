dotfiles_requirements:
  - fd-find
  - fzf
  - git
  - screen
  - tar
  - zsh

# Technically not required but useful.
dotfiles_tools:
  - curl
  - htop
  - jid
  - jq
  - mc
  - tcpdump
  - tree
  - vim

dotfiles_tools_apt:
  - bind9-dnsutils

dotfiles_tools_dnf:
  - bind-utils
  # For dnf tab completion.
  - sqlite
