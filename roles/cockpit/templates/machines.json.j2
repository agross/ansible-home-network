{
{% for host in cockpit_proxied_hosts %}
  "{{ hostvars[host].ansible_facts.hostname }}": {
    "address": "{{ hostvars[host].ansible_facts.hostname }}",
    "visible": "true"
  }{{ "," if not loop.last else "" }}
{% endfor %}
}
