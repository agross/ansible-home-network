borgmatic_borgbackup_install:
  borg_version: ">=1.4.5"
  borgmatic_version: ">=2.1.7"
  method: pip

borgmatic_borgbackup_timer:
  type: ""

borgmatic_borgbackup_ssh:
  key_name: "{{ borgmatic_ssh_key_name }}"
  key_path: "{{ borgmatic_ssh_key_path }}"
  command: "{{ borgmatic_ssh_command }}"

borgmatic_borgbackup_config:
  repositories:
    - "{{ borgmatic_repository }}"
  source_directories:
    - /etc
  one_file_system: false
  store_atime: false
  relocated_repo_access_is_ok: true
  remote_path: "{{ borgmatic_borg_remote_path }}"
  encryption_passphrase: "{{ borgmatic_encryption_passphrase }}"
  hooks:
    healthchecks:
      ping_url: "{{ healthcheck.data.ping_url }}"
  extra: "{{ borgmatic_extra_config | default({}) }}"
