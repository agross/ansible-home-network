[Unit]
Description=Mount {{ share_path }} for smb.service
# Having Before= here and RequiredBy= below is the same as specifying
# RequiresMountsFor= in smb.service.
Before=smb.service

[Mount]
What={{ share.mount.what }}
Where={{ share_path }}

[Install]
RequiredBy=smb.service
