- name: Copy Dockerfile
  template:
    src: ../templates/image/Dockerfile
    dest: "{{ image_dir }}"

- name: Build custom image
  docker_image:
    name: "{{ image_name }}"
    source: build
    build:
      path: "{{ image_dir }}"
      pull: true
  notify: Restart service
