- name: Restart service
  systemd:
    name: "{{ service.name }}.service"
    state: restarted

- name: Restart backup timer
  systemd:
    name: "{{ service.name }}-backup.timer"
    state: restarted
