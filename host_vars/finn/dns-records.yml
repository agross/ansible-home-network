dns_cname_records:
  - mqtt
  - "mqtt.{{ network.domain }}"

  - "influxdb.{{ network.domain }}"

  - "grafana.{{ network.domain }}"
  - "iobroker.{{ network.domain }}"
  - "lovelace.{{ network.domain }}"
  - "gast.{{ network.domain }}"
