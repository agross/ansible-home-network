logger_backup_healthcheck: "{{ vault_logger_backup_healthcheck }}"

logger_grafana_admin_password: "{{ vault_logger_grafana_admin_password }}"

logger_grafana_google_client_id: "{{ vault_logger_grafana_google_client_id }}"
logger_grafana_google_client_secret: "{{ vault_logger_grafana_google_client_secret }}"
logger_grafana_google_allowed_domains: "{{ vault_logger_grafana_google_allowed_domains }}"

logger_grafana_smtp_user: "{{ google_smtp_user }}"
logger_grafana_smtp_password: "{{ vault_logger_grafana_smtp_password }}"

# The API key needs to have the "Admin" role.
logger_grafana_backup_api_key: "{{ vault_logger_grafana_api_key }}"
logger_grafana_backup_admin_user: "{{ vault_logger_grafana_admin_user }}"
logger_grafana_backup_admin_password: "{{ vault_logger_grafana_admin_password }}"

logger_influxdb_init_password: "{{ vault_influxdb_init_password }}"
logger_influxdb_init_admin_token: "{{ vault_influxdb_init_admin_token }}"
logger_influxdb_init_org: "{{ site }}"
logger_influxdb_backup_buckets:
  - iobroker/current
