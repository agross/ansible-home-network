interfaces:
  lan:
    device: enp6s18
    ip4: "{{ network.ipv4 | ansible.utils.ipaddr('3') }}"
    dns4: 127.0.0.1
    dns4_search: "{{ dns.search_domains }}"
    ip6: "{{ network.ipv6 | ansible.utils.ipaddr('3') }}"
    dns6: ::1
    dns6_search: "{{ dns.search_domains }}"
    zone: internal
