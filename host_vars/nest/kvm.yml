kvm:
  host: pve-home
  id: 108
  startup: order=200
  cores: 8
  memory: 8192
  net:
    net0: virtio,bridge=vmbr0,firewall=1
  efi_disk_storage: local-zfs
  scsi:
    scsi0:
      storage: local-zfs
      size: 50
      discard: true
      iothread: true
      ssd: true
