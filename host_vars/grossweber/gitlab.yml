gitlab_backup_healthcheck: "{{ vault_gitlab_backup_healthcheck }}"

gitlab_domain: gitlab.grossweber.com

gitlab_initial_root_password: "{{ vault_gitlab_initial_root_password }}"

gitlab_smtp_server: smtp-relay.gmail.com
gitlab_smtp_user_name: "{{ vault_gitlab_smtp_user_name }}"
gitlab_smtp_password: "{{ vault_gitlab_smtp_password }}"

gitlab_trusted_proxies:
  - "{{ hostvars['proxy-home'].network.ip4 }}"
  - "{{ hostvars['proxy-home'].network.ip6 }}"
