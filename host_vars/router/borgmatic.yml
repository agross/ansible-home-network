borgmatic_encryption_passphrase: "{{ vault_borgmatic_encryption_passphrase }}"

borgmatic_healthchecks_system: "{{ vault_borgmatic_healthchecks_system }}"
borgmatic_healthchecks_harbor: "{{ vault_borgmatic_healthchecks_harbor }}"
borgmatic_healthchecks_vaultwarden: "{{ vault_borgmatic_healthchecks_vaultwarden }}"
borgmatic_healthchecks_nextcloud: "{{ vault_borgmatic_healthchecks_nextcloud }}"
borgmatic_healthchecks_immich: "{{ vault_borgmatic_healthchecks_immich }}"
borgmatic_healthchecks_freshrss: "{{ vault_borgmatic_healthchecks_freshrss }}"
borgmatic_healthchecks_commafeed: "{{ vault_borgmatic_healthchecks_commafeed }}"
borgmatic_healthchecks_banking: "{{ vault_borgmatic_healthchecks_banking }}"
