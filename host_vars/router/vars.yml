adguard_users: "{{ vault_adguard_users }}"

vaultwarden_mariadb_root_password: "{{ vault_vaultwarden_mariadb_root_password }}"
vaultwarden_mariadb_password: "{{ vault_vaultwarden_mariadb_password }}"
vaultwarden_admin_token: "{{ vault_vaultwarden_admin_token }}"

photoprism_mariadb_root_password: "{{ vault_photoprism_mariadb_root_password }}"
photoprism_mariadb_password: "{{ vault_photoprism_mariadb_password }}"
photoprism_admin_password: "{{ vault_photoprism_admin_password }}"

harbor_admin_password: "{{ vault_harbor_admin_password }}"
harbor_database_password: "{{ vault_harbor_database_password }}"

borgmatic_healthchecks_etc: "{{ vault_borgmatic_healthchecks_etc }}"
borgmatic_healthchecks_harbor: "{{ vault_borgmatic_healthchecks_harbor }}"
borgmatic_healthchecks_vaultwarden: "{{ vault_borgmatic_healthchecks_vaultwarden }}"
borgmatic_healthchecks_nextcloud: "{{ vault_borgmatic_healthchecks_nextcloud }}"
