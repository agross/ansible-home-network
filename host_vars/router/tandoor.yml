tandoor_backup_healthcheck: "{{ vault_tandoor_backup_healthcheck }}"

tandoor_secret_key: "{{ vault_tandoor_secret_key }}"

tandoor_smtp_user: "{{ google_smtp_user }}"
tandoor_smtp_password: "{{ vault_tandoor_smtp_password }}"
