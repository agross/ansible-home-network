device_name: "{{ (ansible_host == 'localhost') | ternary(inventory_hostname, ansible_host) }}"

tasmota_password: "{{ vault_tasmota_password }}"

# Device function, e.g. power, lights, climate.
function: power

friendly_name: >-
  {{ device_model }}
  #{{ inventory_hostname_short | regex_search('(?<=-)\d+$') }}
  {{ room }}
  {% if installation_location is defined %}{{ installation_location }} {% endif %}{{ purpose }}
