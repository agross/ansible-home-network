# {{ ansible_managed }}
http:
  routers:
    {{ ansible_role_name }}:
      entrypoints: https
      rule: Host(`cockpit.{{ network.domain }}`)
      service: {{ ansible_role_name }}

  services:
    {{ ansible_role_name }}:
      loadBalancer:
        servers:
          - url: http://localhost:{{ cockpit_port }}
